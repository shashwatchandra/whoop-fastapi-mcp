# Copy this file to .env and fill values
SESSION_SECRET=change_this_random_string

WHOOP_CLIENT_ID=your_whoop_client_id_here
WHOOP_CLIENT_SECRET=your_whoop_client_secret_here

WHOOP_AUTH_URL=https://api.prod.whoop.com/oauth/oauth2/authorize
WHOOP_TOKEN_URL=https://api.prod.whoop.com/oauth/oauth2/token
WHOOP_API_BASE=https://api.prod.whoop.com

# For local dev, use an HTTPS tunnel (e.g., ngrok) and paste it here:
PUBLIC_BASE_URL=https://<your-https-tunnel>
# If you prefer to set redirect directly, comment the line above and set:
# WHOOP_REDIRECT_URI=https://<your-https-tunnel>/callback
